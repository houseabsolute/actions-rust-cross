[tools]
node = "26.5.1"
"npm:oxfmt" = "0.61.0"
# Held back: ruff 0.16 enables new rules that the Python helpers here do not
# yet satisfy. Bump this once those are cleaned up.
"github:astral-sh/ruff" = "0.15.17"
shellcheck = "0.11.0"
shfmt = "3.13.1"
typos = "1.48.0"
"github:houseabsolute/omegasort" = "0.1.3"
"github:houseabsolute/precious" = "0.11.0"

[settings]
minimum_release_age = "3d"
